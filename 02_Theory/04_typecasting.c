#include <stdio.h>
int main() {
    int n= 45;
    float f= 4.5;
    // Type casting n to float and f to int
    printf("Value of n as float: %.2f\n",(float) n);
    printf("Value of f as int: %d\n", (int)f); 
    return 0;
}