#include <stdio.h>
int main() {
    int a =5;
    
    if (a>=10){
        printf("%d is greater than or equal to 10",a);
    }
    else{
        printf("%d is less than 10",a);
    }

    return 0;
}